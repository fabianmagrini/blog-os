# agent-harnesses (abandoned worked example)

**status: abandoned.** This article workspace was created to demonstrate the
full per-article layout (`docs/init-spec.md` §30) and give the pipeline
something concrete to run against — Gate 2 (thesis approval) was never
reached; `thesis_approved` stayed `false` throughout. It's kept here rather
than deleted so the worked example (including the real Scout-sourced signal
linked to `idea-001` in `ideas/backlog.yaml`) stays available for reference.

The underlying idea (`idea-001`, `ideas/backlog.yaml`) is not rejected on its
merits — it's simply not the one being pursued right now. It remains at
`status: shortlisted` and can be picked back up later via
`workflows/develop.md`.

A new article is starting from `workflows/discover.md` / `workflows/develop.md`
for a real author-supplied idea.
